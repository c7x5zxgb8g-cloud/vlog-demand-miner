{
  "_comment": "NextTake Content Engine SessionStart hook. Auto-renders 4-6 line status report at every new conversation. Synchronous so the report appears in Claude's context BEFORE first reply. Silently exits if not in a NextTake Content Engine project (no .nexttake-state.json found).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PROJECT_DIR}/.nexttake-hooks/session-start.sh",
            "timeout": 5,
            "async": false
          }
        ]
      }
    ]
  }
}
